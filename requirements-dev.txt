# ---------------------------------------------------------------------------
# MiniLang Compiler Visualizer — development dependencies
#
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# These are what CI (.github/workflows/ci.yml) runs: ruff, black, pytest.
# ---------------------------------------------------------------------------

-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient.TestClient
black>=24.0
ruff>=0.6
